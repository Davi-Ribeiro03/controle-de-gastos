import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    backgroundColor: "#DAEFFF",
    width: "100%",
    height: 50,
    borderRadius: 4,
    paddingLeft: 10,
    marginVertical: 10,
  },
});
